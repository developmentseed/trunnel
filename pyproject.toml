[tool.uv]
managed = true
dev-dependencies = [
    "trunnel-cli[types]",
    "pytest>=8.0.0",
    "pytest-mock>=3.14.0",
    "ruff>=0.6.0",
    "ty>=0.0.18",
]

[tool.uv.sources]
trunnel-cli = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.ruff.lint.isort]
combine-as-imports = true
known-first-party = [
    "trunnel_cli",
    "trunnel_infra",
]
